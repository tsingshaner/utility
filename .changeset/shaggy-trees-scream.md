---
'@qingshaner/utility-orpc': minor
---

Add `generateErrorStatusMap` to scan TypeScript source directories for namespaced error codes and generate a typed HTTP status map. Unknown prefixes default to status 500, and unchanged output files are preserved.
