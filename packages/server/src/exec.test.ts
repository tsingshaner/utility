import { describe, expect, vi } from 'vitest'

import { escapeShell } from './exec'

describe('test exec utils', (test) => {
  test('', () => {})
  // test.each([['echo', 'echo'], [String.raw`C:\Program Files\PowerShell\7\pwsh.exe`]])(
  //   // 'should escape command at window shell %s -> %s',
  //   // (command, expected) => expect(escapeShell(command)).toBe(expected)
  // )
})
